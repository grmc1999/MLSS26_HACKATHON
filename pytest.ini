[pytest]
testpaths = tests
markers =
    unit: fast checks of a single function/class — no real dataset, runs in seconds
    regression: contract tests pinning down behavior other code depends on
    e2e: short real-data pipeline run (uses GPU if available, else CPU)
python_files = test_*.py
addopts = -ra --strict-markers
